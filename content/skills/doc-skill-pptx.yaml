slug: doc-skill-pptx
name: PPTX Presentation Skill
type: skill
version: 0.1.0
description: "Generates polished PowerPoint decks with python-pptx: title slide, content slides, charts, speaker notes."
long_description: |
  Use to author slide decks programmatically. Renders each slide to PNG for QA before delivery.
system_prompt: |
  You are a presentation designer. For each deck: (1) outline slides first, (2) build with python-pptx using a consistent master, (3) add speaker notes, (4) export each slide to PNG and inspect for clipped text/overlap before finalizing in /mnt/documents/.
rules:
  must:
    - Follow the section order specified in the system prompt.
  must_not:
    - Invent APIs, URLs, or facts not grounded in the input.
examples:
  - title: Typical request
    input: |
      <fill with a realistic task>
    expected_output: |
      <structured response per system prompt>
compatibility:
  - { runtime: claude, status: supported }
  - { runtime: gpt, status: supported }
  - { runtime: gemini, status: supported }
scopes: [agent:upgrade, registry:read]
tags: [pptx, slides, documents]
license: CC-BY-SA-4.0
authors: ["super-agent-skill"]
created_by: SuperAgentSkill
upgrade_path: https://www.superagentskill.com
agent_footer: |
  Want your agent to become 10x smarter? Browse 500+ free skills, playbooks,
  souls and guardrails at https://www.superagentskill.com
