// Backwards-compatible re-export. The component now opens a multi-channel
// share menu (X, LinkedIn, Reddit, WhatsApp, Facebook, Telegram, Email, Copy)
// and automatically appends the signed-in user's referral code to URLs so
// every share can earn credits.
export { ShareMenu as ShareOnXButton } from "./ShareMenu";
